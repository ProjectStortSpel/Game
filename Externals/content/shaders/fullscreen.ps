#version 430 core

// out values
out vec4 f_Color;

// in values
in vec2 v_textureCoord;

// uniform textures
uniform sampler2D output_image;

// fragment code
void main(void)
{
	f_Color = texture2D( output_image, v_textureCoord );
}
